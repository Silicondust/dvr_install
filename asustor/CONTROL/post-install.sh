#!/bin/sh
hdhr_arch_log=/tmp/hdhr_arch_choice

echo "---post installing---" >> $hdhr_arch_log
echo "repo $APKG_REPO_DIR" >> $hdhr_arch_log
echo "repo $APKG_REPO_DIR" >> $hdhr_arch_log
echo "repo $APKG_REPO_DIR" >> $hdhr_arch_log
echo "repo $APKG_REPO_DIR" >> $hdhr_arch_log
